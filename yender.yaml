services:
  - type: web
    name: hrms-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn hrms_backend.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: hrms_backend.settings
